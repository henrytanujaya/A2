# ─── Frontend Environment Variables ─────────────────────────────────────────
# Copy file ini menjadi .env dan sesuaikan nilainya

# URL backend API (untuk local development)
# Jika tidak diset, proxy di vite.config.js akan otomatis forward ke localhost:8321
VITE_API_BASE_URL=http://localhost:8321
